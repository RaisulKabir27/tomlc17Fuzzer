# tomlc17Fuzzer
An LLM-guided black-box fuzzer for the tomlc17 TOML parser. An ANTLR grammar becomes a Hypothesis generator, run against a sanitizer-built C harness and refined over five iterations by adaptive proxy-signal feedback — no coverage instrumentation.
